diff --git a/./CMakeLists.txt b/./CMakeLists.txt
--- ./CMakeLists.txt
+++ ./CMakeLists.txt
@@ -11,6 +11,27 @@
 set (PROJECT_AUTHORS "Larry Gritz, et al")
 set (PROJECT_URL "http://openimageio.org")
 
+if(WIN32)
+    file(TO_CMAKE_PATH ${CMAKE_INSTALL_PREFIX} CMAKE_INSTALL_PREFIX)
+    file(TO_CMAKE_PATH ${CMAKE_PREFIX_PATH} CMAKE_PREFIX_PATH)
+    file(TO_CMAKE_PATH ${OPENEXR_INCLUDE_PATH} OPENEXR_INCLUDE_PATH)
+    file(TO_CMAKE_PATH ${OPENEXR_IMATH_LIBRARY} OPENEXR_IMATH_LIBRARY)
+    file(TO_CMAKE_PATH ${OPENEXR_ILMIMF_LIBRARY} OPENEXR_ILMIMF_LIBRARY)
+    file(TO_CMAKE_PATH ${OPENEXR_IEX_LIBRARY} OPENEXR_IEX_LIBRARY)
+    file(TO_CMAKE_PATH ${OPENEXR_ILMTHREAD_LIBRARY} OPENEXR_ILMTHREAD_LIBRARY)
+    file(TO_CMAKE_PATH ${ZLIB_INCLUDE_DIR} ZLIB_INCLUDE_DIR)
+    file(TO_CMAKE_PATH ${ZLIB_LIBRARY} ZLIB_LIBRARY)
+    file(TO_CMAKE_PATH ${PNG_PNG_INCLUDE_DIR} PNG_PNG_INCLUDE_DIR)
+    file(TO_CMAKE_PATH ${PNG_LIBRARY} PNG_LIBRARY)
+    file(TO_CMAKE_PATH ${JPEG_INCLUDE_DIR} JPEG_INCLUDE_DIR)
+    file(TO_CMAKE_PATH ${JPEG_LIBRARY} JPEG_LIBRARY)
+    file(TO_CMAKE_PATH ${TIFF_INCLUDE_DIR} TIFF_INCLUDE_DIR)
+    file(TO_CMAKE_PATH ${TIFF_LIBRARY} TIFF_LIBRARY)
+    file(TO_CMAKE_PATH ${PYTHON_INCLUDE_DIR} PYTHON_INCLUDE_DIR)
+    file(TO_CMAKE_PATH ${PYTHON_LIBRARY} PYTHON_LIBRARY)
+    file(TO_CMAKE_PATH ${OCIO_LIBRARY_PATH} OCIO_LIBRARY_PATH)
+endif()
+
 message (STATUS "CMake version is ${CMAKE_VERSION}")
 cmake_policy (SET CMP0017 NEW)  # Prefer files from the CMake module directory when including from there.
 cmake_policy (SET CMP0025 NEW)  # Detect AppleClang for new CMake
