--- a/tools/clang/lib/Tooling/CMakeLists.txt
+++ b/tools/clang/lib/Tooling/CMakeLists.txt
@@ -24,6 +24,7 @@ string(SUBSTRING ${CMAKE_CURRENT_BINARY_DIR} ${PATH_LIB_END} -1 PATH_TAIL)
 string(CONCAT BINARY_INCLUDE_DIR ${PATH_HEAD} "/include/clang/" ${PATH_TAIL})
 
 if (NOT Python3_EXECUTABLE
+    OR WIN32
     OR APPLE
     OR CMAKE_CROSSCOMPILING
     OR GENERATOR_IS_MULTI_CONFIG
